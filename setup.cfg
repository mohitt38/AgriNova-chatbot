[metadata]
name = agri-nova-chatbot
version = 0.0.1
description = Local packaging wrapper for AgriNova chatbot (keeps imports as `src`)

[options]
package_dir =
    = src
packages = find:
 
[options.packages.find]
where = src
